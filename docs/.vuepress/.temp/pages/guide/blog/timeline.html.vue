<template><div><p>你可以通过 <code v-pre>/timeline/</code> 访问时间页面，在页面中，你可以按照时间倒序查看所有的文章列表。</p>
<p><img src="@source/guide/blog/assets/timeline-light.png" alt="时间线列表" data-mode="lightmode-only" loading="lazy">
<img src="@source/guide/blog/assets/timeline-dark.png" alt="时间线列表" data-mode="darkmode-only" loading="lazy"></p>
<h2 id="排除文章" tabindex="-1"><a class="header-anchor" href="#排除文章" aria-hidden="true">#</a> 排除文章</h2>
<p>默认情况下所有注明了写作日期的文章都会显示时间线中。如果你不希望某篇文章被包含，只需在文章的 frontmatter 中将 <code v-pre>timeline</code> 设置为 <code v-pre>false</code>。</p>
<h2 id="配置时间线顶部文字" tabindex="-1"><a class="header-anchor" href="#配置时间线顶部文字" aria-hidden="true">#</a> 配置时间线顶部文字</h2>
<p>我们允许你定制时间线的顶部文字，你可以在主题选项中通过 <code v-pre>blog.timeline</code> 配置它。</p>
<h2 id="侧边栏" tabindex="-1"><a class="header-anchor" href="#侧边栏" aria-hidden="true">#</a> 侧边栏</h2>
<p>我们在博客相关页面的侧边栏添加了时间线选项卡，供用户快速预览时间线。</p>
<p><img src="@source/guide/blog/assets/timeline-tab.png" alt="时间线选项卡" loading="lazy"></p>
</div></template>


